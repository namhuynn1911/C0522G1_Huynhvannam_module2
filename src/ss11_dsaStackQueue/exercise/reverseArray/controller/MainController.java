package ss11_dsaStackQueue.exercise.reverseArray.controller;

import ss11_dsaStackQueue.exercise.reverseArray.model.ReverseArray;

public class MainController {
    public static void main(String[] args) {
       // ReverseArray.isInteger();
        ReverseArray.isString();
    }
}
